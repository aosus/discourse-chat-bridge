{
  "name": "discourse-chat-bridge",
  "description": "bridge between discourse and social media programs",
  "version": "1.0.0",
  "main": "index.js",
  "type": "module",
  "scripts": {
    "start": "node index.js",
    "generate_matrix_token": "node ./matrix/generate_matrix_token.js"
  },
  "author": "aosus",
  "license": "agpl-3.0",
  "dependencies": {
    "dotenv": "^17.0.1",
    "fs-extra": "^11.1.1",
    "matrix-bot-sdk": "^0.7.1",
    "moment-hijri": "^3.0.0",
    "node-fetch": "^3.3.1",
    "telegraf": "^4.12.2"
  },
  "devDependencies": {
    "@types/fs-extra": "^11.0.1",
    "@types/moment-hijri": "^2.1.0"
  },
  "overrides": {
    "tough-cookie": "^4.1.3"
  },
  "security": {
    "notes": "4 moderate vulnerabilities remain from matrix-bot-sdk dependency chain (request library). These are legacy dependencies that cannot be updated without changing the matrix-bot-sdk library. The vulnerabilities are in deprecated request/request-promise packages that are no longer maintained."
  }
}
